{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd inversiones-bot && npm install --omit=dev"
  },
  "deploy": {
    "startCommand": "cd inversiones-bot && node bot.js",
    "restartPolicyType": "ON_FAILURE",
    "numReplicas": 1
  }
}